---
import Layout from '../layouts/Layout.astro';

// Text types and their classes
const textTypes = [
  { name: "Default text", classes: "text-foreground" },
  { name: "Muted text", classes: "text-muted" },
  { name: "Link text", classes: "underline hover:text-foreground/80" },
  { name: "Heading 1", classes: "text-3xl font-bold" },
  { name: "Heading 2", classes: "text-2xl font-semibold" },
  { name: "Small text", classes: "text-sm" },
];

// Background types to test against
const backgroundTypes = [
  { name: "Default background", classes: "bg-background" },
  { name: "Card background", classes: "bg-card" },
  { name: "Dark background", classes: "bg-slate-800" },
  { name: "Light background", classes: "bg-slate-100" },
];
---

<Layout title="Accessibility Test" description="Testing accessibility of color contrast ratios">
  <div class="max-w-4xl w-full mx-auto p-4">
    <h1 class="text-3xl font-bold mb-8">Accessibility Color Contrast Test</h1>
    
    <div class="space-y-8">
      <section>
        <h2 class="text-2xl font-semibold mb-4">Color Contrast Matrix</h2>
        <p class="mb-6">
          This page tests various text styles against different backgrounds to ensure proper contrast ratios.
          Per WCAG Guidelines:
        </p>
        <ul class="list-disc pl-6 mb-6">
          <li>Normal text should have a contrast ratio of at least <strong>4.5:1</strong></li>
          <li>Large text (18pt+ or 14pt+ bold) should have a contrast ratio of at least <strong>3:1</strong></li>
        </ul>
        
        <div class="grid gap-6">
          {backgroundTypes.map(bg => (
            <div class={`p-4 rounded-lg ${bg.classes}`}>
              <h3 class="text-xl font-medium mb-3">{bg.name}</h3>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {textTypes.map(text => (
                  <div class="p-3 border border-gray-200 dark:border-gray-700 rounded">
                    <p class="text-sm text-muted mb-2">Testing: {text.name}</p>
                    <p class={text.classes}>
                      This is sample text with the class "{text.classes}"
                    </p>
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      </section>
      
      <section>
        <h2 class="text-2xl font-semibold mb-4">Interactive Elements Focus Testing</h2>
        <p class="mb-4">Tab through these elements to test focus styles:</p>
        
        <div class="flex flex-wrap gap-4 mb-6">
          <a href="#" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">Link Button</a>
          <button class="px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700">Standard Button</button>
          <input type="text" placeholder="Input field" class="px-4 py-2 border rounded" />
          <select class="px-4 py-2 border rounded">
            <option>Select option</option>
            <option>Option 1</option>
            <option>Option 2</option>
          </select>
        </div>
      </section>
      
      <section>
        <h2 class="text-2xl font-semibold mb-4">Skip Link Test</h2>
        <p>Press Tab immediately when the page loads to see if the "Skip to content" link appears properly.</p>
      </section>
      
      <section>
        <h2 class="text-2xl font-semibold mb-4">Testing Tools</h2>
        <ul class="list-disc pl-6 space-y-2">
          <li>
            <a href="https://wave.webaim.org/" target="_blank" rel="noopener" class="underline">
              WAVE Web Accessibility Evaluation Tool
            </a>
          </li>
          <li>
            <a href="https://webaim.org/resources/contrastchecker/" target="_blank" rel="noopener" class="underline">
              WebAIM Contrast Checker
            </a>
          </li>
          <li>
            <a href="https://accessibilityinsights.io/" target="_blank" rel="noopener" class="underline">
              Accessibility Insights
            </a>
          </li>
        </ul>
      </section>
    </div>
  </div>
</Layout> 